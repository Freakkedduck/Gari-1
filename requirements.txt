# --- FastAPI & Web Server ---
fastapi
uvicorn
python-multipart

# --- Basic Text Extraction ---
pdfplumber            # For lightweight PDF text/tables
python-docx           # For DOCX parsing
pillow                # For image processing

# --- Advanced PDF & Layout Parsing ---
PyMuPDF               # Layout-aware PDF extraction (aka fitz)

# --- OCR ---
pytesseract           # OCR from images
pdf2image             # Convert PDF pages to images

# --- Optional Utilities ---
tqdm                  # For progress bars during batch processing

# --- vector databases ---
chromadb
sentence-transformers

# --- frontend ---
streamlit             # For building the web interface